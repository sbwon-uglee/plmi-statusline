# plmi-statusline 작업 규칙

Claude Code statusLine 에 사는 플밍이. 구조와 쓰는 법은 `README.md`.
왜 이렇게 되어 있는지와 접은 대안은 `docs/DECISIONS.md`.

여기에는 코드를 고칠 때 지켜야 하는 것만 적는다.

## 실행과 굽기의 경계

`statusline.py`, `bubble.py`, `summary.py`, `state.py`, `play.py`, `install.py` 는
표준 라이브러리만 쓴다. 맥 기본 파이썬에서 그냥 도는 것이 배포 조건이다. numpy 와 pillow 는
`anim.py`, `dot.py`, `build_sprites.py`, `dump_frames.py` 쪽에만 둔다.

`statusline.py` 는 스프라이트를 `__file__` 기준으로 찾는다. 상대경로나 cwd 에 기대면
설치한 사람의 프로젝트 폴더에서 깨진다.

## 브라유 격자의 한계

한 칸은 가로 2점 세로 4점이고, 터미널은 칸마다 앞색과 배경색을 하나씩만 준다.
안 켠 점에는 색을 실을 수 없다.

- 칸 배경색을 쓰지 않는다. 브라유 점이 칸을 다 채우지 않아 배경이 칸 전체를 덮는다.
- 눈과 입은 파낸다. 파낸 자리는 터미널 배경이라 어둡게 나오고, 그 대신 모양이 점 단위로 남는다.
- 눈은 26칸에서 3x3점, 20칸에서 2x2점이다. 이 크기에서 읽히는 모양만 그린다.

## 색을 얹을 때

`anim.py` 의 `GAIN` 이 칸 색을 몸통 평균에서 세 배로 벌린다. 삼각면 무늬를 보이게 하려는
것이다. 원화 색을 그대로 찍으면 세 배로 밀려 튄다. `fit_color(gain=)` 로 미리 나눠 찍어야
벌린 뒤에 원하는 값으로 떨어진다. 볼 진하기는 `BLUSH` 로 조절한다.

## 격자 기하는 Layout 하나가 정한다

`fit`, `face_art`, `fit_color` 는 `Layout` 을 받는다. 배율, 기준 크기, 자세를 준 크기,
오프셋, 원화 좌표 되돌리기가 전부 그 안에 있다. 셋이 같은 Layout 을 받으므로 얼굴과
몸과 색이 다른 자리를 잡을 수 없다.

전에는 셋이 같은 여덟 줄을 각자 세웠다. 한 곳만 고치면 나머지 둘이 어긋났고, 볼이 입에서
떨어지거나 눈 옆에 올리브색 얼룩이 남은 것이 전부 여기서 나왔다. 기하를 함수 안에 다시
풀어 쓰면 `tests/test_layout.py` 가 막는다.

새 배율이 필요하면 `Layout.fill` 을 쓴다. `scale_for` 와 `headroom_scale` 도 그것을 부른다.

## 얼굴

`dot.face_art` 가 원화에서 자리와 크기만 재고, 모양은 `EYE_ART` 와 `MOUTH_ART` 패턴으로
점 격자에 직접 찍는다.

- `face0` 은 원화 눈과 입의 자리다. 색 계산에서 항상 뺀다. 새 모양이 원화보다 작으면
  안 덮인 원화 눈(밝기 85)이 몸통(228)으로 세어져 눈 옆 칸이 올리브색 얼룩이 된다.
- 눈 상자 배율(`EYE_BOX`)은 눈 짝마다 그 눈이 쓸 모양으로 잡는다. 표정 단위로 주면
  한쪽만 커야 할 배율이 반대쪽 눈에도 걸린다.

## 볼

두 짝이 한 줄을 같이 쓴다. 원화 입 윗변에 맞추고, 눈에 걸리면 볼을 통째로 바깥으로 비킨다.
막는 자리는 원화 눈과 입(`face0`)에 그려 넣은 눈과 입을 합쳐서 본다.

## 격자 크기

`headroom_scale` 이 배율을 정한다. 늘어난 프레임은 바닥을 붙인 채 위로만 자라므로 남는
자리는 격자 높이가 아니라 위쪽 여백으로 따진다.

줄 수는 `--rows` 를 안 주면 자동으로 잡는다. 세로가 아니라 가로가 배율을 정할 때까지 줄을
늘리고, 늘 비는 줄은 뒤에서 잘라 낸다. 딱 맞게 잡으면 뛰어오른 프레임을 받을 자리가 없어
배율이 줄고 캐릭터가 작아진다.

## 굽는 절차

`./build.sh` 로 굽는다. 옛 스프라이트를 먼저 지우고, 다 구운 뒤 검사를 돌린다.
줄 수는 빈 줄을 잘라낸 뒤 정해지므로 같은 인자로도 결과 이름이 달라지고, 옛 이름의
파일이 남으면 그것을 보게 된다.

고쳤다고 말하기 전에 `python3 tests/run.py` 가 통과해야 한다. 검사를 고치거나 더하면
`python3 tests/mutate.py` 로 그 검사가 실제로 잡는지 확인한다.

눈으로 확인할 때는 `dump_frames.py` 로 전 프레임을 HTML 에 뽑는다. 터미널과 같게 보려면
줄 높이를 글꼴에 맞춘다. JetBrains Mono 는 자간 600/1000 em 에 줄 1320/1000 em 이라
`line-height: 1.32` 다.

## 글쓰기

주석, 커밋 메시지, 문서, 사용자에게 보이는 문구 전부 사람이 키보드로 치는 문자만 쓴다.
em dash 와 가운뎃점을 쓰지 않는다. 나열은 쉼표, 계층 구분은 슬래시로 푼다.
