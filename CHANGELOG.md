# 바뀐 내역

새 버전을 낼 때 맨 위에 항목을 추가합니다. 쓰는 사람이 무엇을 다르게 겪는지를 적고,
코드 안쪽 사정은 `docs/DECISIONS.md` 에 둡니다.

## 0.4.3 (2026-09-11)

- **공백이 든 폴더에 받아도 상태줄이 나옵니다.** 클론을 「내 폴더」 같은 자리에 받으면
  아무 표시 없이 비어 있었습니다. brew 로 깐 경우는 해당하지 않았습니다

## 0.4.2 (2026-09-11)

- **클론해 쓰다 brew 로 옮겨도 그냥 `plmi` 로 붙습니다.** 다른 사본이 붙인 상태줄을 남의 것으로
  보고 `--force` 를 요구하던 자리입니다. 플밍이가 아닌 상태줄은 전처럼 막습니다

## 0.4.1 (2026-09-10)

- 저장소 작업 규칙에서 가운뎃점 금지를 뺐습니다. 코드가 하는 일은 그대로입니다

## 0.4.0 (2026-09-10)

- **오래 쉬어도 화난 얼굴이 되지 않습니다.** 눈은 평소대로 뜨고 입만 시무룩해집니다.
  말풍선으로 심심하다고 합니다. 세션을 여러 개 띄워 두면 안 쓰는 것마다 찌푸린 얼굴이
  되던 자리입니다
- **어느 사본이 붙였든 뗍니다.** 저장소에서 쓰던 것이나 옛 판이 붙인 상태줄은
  `--uninstall` 이 남의 것으로 보고 거부했습니다

## 0.3.1 (2026-09-10)

- 미리 보기 첫 줄에 쓰던 가운뎃점을 쉼표와 마침표로 바꿨습니다. 화면에 보이는 문구는
  사람이 키보드로 치는 문자만 씁니다

## 0.3.0 (2026-09-10)

- **붙이기 전에 미리 볼 수 있습니다.** `plmi --preview` 가 그 크기로 몇 초 돌려 줍니다.
  전에는 붙이고 Claude Code 를 다시 띄워야 처음 봤고, 크기가 안 맞으면 그 왕복을 반복했습니다.
  `--state 놀람` 처럼 표정도 고를 수 있습니다
- **떼는 순서를 바꿔도 상태줄이 깨지지 않습니다.** `plmi --uninstall` 없이 `brew uninstall` 을
  하면 상태줄 자리에 파일을 못 찾겠다는 오류가 찍혔습니다. 이제 조용히 비기만 합니다

## 0.2.1 (2026-09-10)

- **일하는 중에 뾰로통해지지 않습니다.** 도구를 한참 돌리는 중이라도 그 앞 답변이 오래됐으면
  삐진 얼굴이 떴습니다. 조용한지는 마지막에 무슨 일이든 일어난 시각으로 봅니다
- **긴 대화에서도 지금 뭘 하는지 맞게 나옵니다.** 도구 결과 한 줄이 읽는 창보다 커서, 그런
  줄이 끝에 오면 아무것도 못 찾고 가만히 있는 얼굴이 됐습니다
- **말을 걸면 바로 알아봅니다.** 어떤 형태로 오는 사람 말은 통째로 못 보고 있었습니다

## 0.2.0 (2026-09-10)

- **원하는 칸 수로 구울 수 있습니다.** `plmi --bake 30` 이면 30칸짜리가 생기고
  `plmi --size 30x13` 으로 갈아 낍니다. 결과는 `~/.claude/plmi-sizes` 에 들어가서 판을
  올려도 살아남습니다
- **놀람과 뾰로통이 실제로 나옵니다.** 그려서 배포는 했는데 어떤 기록으로도 나오지 않던
  두 상태입니다. 하던 일을 끊으면 놀라고, 15분 넘게 조용하면 뾰로통해집니다
- **창보다 큰 크기는 붙일 때 막습니다.** 그대로 붙이면 줄바꿈으로 그림이 무너졌습니다.
  들어가는 크기를 같이 알려 주고, 그래도 붙이려면 `--force` 입니다
- **떼면 자리가 깨끗해집니다.** 빈 `{}` settings.json 과 백업이 워크스페이스마다 남았습니다.
  다른 설정이 들어 있으면 파일은 그대로 둡니다
- **`PLMI_SIZE` 를 안 줘도 맞는 크기가 나옵니다.** 없어진 크기를 찾다가 아무거나 집던
  자리입니다
- 환경변수 세 개와 여덟 상태가 각각 언제 나오는지를 README 에 적었습니다

## 0.1.3 (2026-09-10)

- **붙일 워크스페이스를 지목할 수 있습니다.** `plmi --scope project --dir ~/work/foo` 입니다.
  전에는 붙이려는 워크스페이스마다 그 폴더로 옮겨 가야 했습니다
- **`plmi --where` 가 붙어 있는 자리를 찾아 줍니다.** 떼려면 어디에 붙였는지 알아야 하는데
  여러 워크스페이스에 붙이면 사람이 기억하고 있어야 했습니다. brew 로 깐 것과 클론해 쓰는
  것을 함께 찾습니다

## 0.1.2 (2026-09-10)

- **`LICENSE` 가 생겼습니다.** 사내용입니다. 저장소를 공개로 둔 것은 Homebrew 가 tarball 을
  인증 없이 받게 하려는 것이고 오픈소스로 낸 것이 아닙니다. 회사 구성원과 지정한 협력자는
  설치하고 고쳐 쓸 수 있고, 그 밖의 배포와 상업적 이용은 사전 동의가 필요합니다

## 0.1.1 (2026-09-10)

0.1.0 은 brew 로 깔아도 `plmi` 명령이 돌지 않았습니다.

- **`brew install` 뒤에 `plmi` 가 실제로 실행됩니다.** 명령을 심볼릭 링크로 걸어 두었는데
  가리키는 파일에 셰뱅이 없어 셸이 실행할 방법을 몰랐습니다
- **파이썬을 따로 받지 않습니다.** 없는 이름의 의존을 걸어 두어 설치가 막혔습니다. 상태줄을
  그리는 쪽은 표준 라이브러리만 써서 맥에 있는 python3 로 충분합니다

## 0.1.0 (2026-09-09)

brew 로 깔 수 있는 첫 판입니다.

- **`brew install sbwon-uglee/plmi/plmi` 로 깝니다.** 저장소를 클론하거나 tarball 을 받던
  단계가 없어집니다. 올릴 때는 `brew upgrade plmi` 입니다
- **판을 올려도 상태줄이 안 깨집니다.** 설정에 적히는 경로에서 버전이 빠졌습니다. 전에는
  올리면 옛 폴더가 지워지면서 상태줄이 아무 말 없이 사라졌을 자리입니다
- **`plmi --version` 이 깔린 판을 알려 줍니다**
- **캐릭터 아래에 빈 줄이 한 줄 생깁니다.** 발이 상태줄 바로 밑 모드 표시줄과 맞닿아
  보이던 것을 띄웠습니다. 크기는 그대로입니다
- **크기 이름이 바뀝니다.** 36x16, 26x11, 20x10, 16x8 입니다. 아래 빈 줄이 한 줄씩 늘었습니다

## 0.0.0 (2026-09-09)

버전을 다시 세는 첫 지점입니다. 아직 배포판이 아닙니다.

- **Claude Code 상태줄에 플밍이가 삽니다.** 여덟 상태(숨쉬기, 작업중, 생각중, 승인대기,
  완료, 오류, 놀람, 뾰로통)를 상태줄 폭에 맞춰 브라유 점으로 그립니다
- **네 가지 크기를 미리 구워 둡니다.** 36x16, 26x11, 20x10, 16x8 이고 `PLMI_SIZE` 로 고릅니다
- **설치기가 남의 설정을 건드리지 않습니다.** 쓰기 전에 백업하고, 다른 상태줄이 이미 있으면
  멈춥니다. `--uninstall` 로 되돌립니다
- **맥 기본 파이썬에서 그냥 돕니다.** 상태줄을 그리는 쪽은 표준 라이브러리만 씁니다
